package salies.server.plotter;

public enum FunctionType {
    // y = ax + b
    LINE,
    // y = ax^2 + bx + c
    PARABOLA,
    // y = ax^3 + bx^2 + cx + d
    CUBIC,
    // y = sin(x), x em graus
    SIN,
    // y = cos(x), x em graus
    COS
}
